server:
  port: 8090

eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_SERVER_HOST:localhost}:8761/eureka/

spring:
  application:
    name: admin-service

logging:
  level:
    root: INFO
    com.transactsphere.admin: DEBUG
